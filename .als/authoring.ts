// Pass-through helpers — replace with the ALS compiler's typed authoring surface for stricter literal-type checking.

export const defineSystem = <T>(value: T): T => value;
export const defineModule = <T>(value: T): T => value;
export const defineDelamain = <T>(value: T): T => value;
