---
id: KF-002
title: Add CLI command to set fleet orchestration turn limit
description: Operator-facing CLI command to configure the maximum number of orchestration turns the fleet will run per task.
type: feature
status: done
created: 2026-05-06
updated: 2026-05-06
tags:
  - cli
  - orchestration
  - fleet
research_session: 9b260671-fabb-44ec-b5f2-e7b486f70f01
planner_session: 015f6801-e075-476e-ace1-b6bf67d37a03
dev_session: d4aaac4e-6dbb-45fc-94d5-fb64ba7e280b
testing_session: null
---

# KF-002: Add CLI command to set fleet orchestration turn limit

## PURPOSE

Operators currently can't tune orchestration turn limits without editing config or code. Expose a CLI command so the limit can be changed quickly per-environment without redeploying.

## CURRENT_STATE

There is no single "fleet orchestration turn limit" today — multiple, partially overlapping turn-cap knobs exist:

- **`worker_max_turns`** (per-agent, identity.yaml) — outer worker continuation loop. After each turn that ends with `STATUS: IN_PROGRESS`, the worker re-prompts up to this many times within a single run. Default 5. Read at `packages/cli/src/orchestration/worker-heartbeat.ts:211` (`getIdentityForRoot(root).worker_max_turns ?? 5`). Type declared at `packages/cli/src/types.ts:138`. No setter helper exists; today operators hand-edit identity.yaml.
- **`heartbeat_max_inner_turns`** (per-agent, identity.yaml) — inner Claude SDK turn cap within one heartbeat call. Default 50. Resolver: `getHeartbeatMaxInnerTurnsForRoot()` in `packages/cli/src/config.ts:164`. Used by `worker-heartbeat.ts:212` and `ceo-heartbeat.ts:361`.
- **Per-agent `maxTurns`** in `agents/<name>/agent.yaml` (sub-agent definition). Default 10. Already operator-tunable via `kyberbot agent create --max-turns` (`packages/cli/src/commands/agent.ts:80`) and shown by `kyberbot agent show` (`agent.ts:150`). Not what the job is about.
- **Hardcoded values**: 30 (telegram, whatsapp), 15 (heartbeat default chat), 1 (brain extractors).
- **Dispatcher (delamain) `maxTurns`** in `.als/modules/.../runtime-manifest.config.json` — separate ALS subsystem, not part of "fleet orchestration."

Persistent settings infrastructure that exists today:
- **`orchestration_settings` SQLite singleton table** at `packages/cli/src/orchestration/db.ts:278`. Columns: `orchestration_enabled`, `heartbeat_interval`, `active_hours_start`, `active_hours_end`. Reader/writer: `packages/cli/src/orchestration/settings.ts`. HTTP endpoints: `GET/PUT /fleet/orch/settings` at `packages/cli/src/server/orchestration-api.ts:689,693`. **No turn-limit field today.** Adding one would require: (1) a column + migration, (2) a field on `OrchestrationSettings` (`packages/cli/src/orchestration/types.ts:205`), (3) wiring into `getOrchestrationSettings/updateOrchestrationSettings`, (4) a consumer change in worker-heartbeat to read from DB.
- **Fleet registry** (`~/.kyberbot/registry.yaml`) — used by `kyberbot fleet defaults --auto-start` (`fleet.ts:517`) but is a fleet-listing/registry concern, not a runtime-config one.

CLI surface today:
- `kyberbot fleet ...` (`packages/cli/src/commands/fleet.ts`): `list`, `register`, `register-remote`, `unregister`, `start`, `stop`, `status`, `defaults`. The closest precedent for a setter is `fleet defaults --auto-start <names>` (registry mutation).
- `kyberbot orch ...` (`packages/cli/src/commands/orch.ts`): wraps `/fleet/orch/*` HTTP endpoints — goals, issues, org, inbox, activity, init, status. Currently has **no** `settings` subcommand even though the HTTP endpoint exists. This is the more semantically natural home for orchestration runtime knobs.

## REQUIREMENTS

Re-targeted after operator answers to the 4 research questions (2026-05-06).

- **Command shape**: `kyberbot fleet set-turns <N>` (writer) and `kyberbot fleet get-turns` (reader). Help text must state explicitly that this tunes `heartbeat_max_inner_turns` (the inner Claude SDK turn cap, default 50) and does NOT touch `worker_max_turns` or per-agent sub-agent `maxTurns`.
- **Field mutated**: `heartbeat_max_inner_turns` in `<agent_root>/identity.yaml`. No schema change anywhere else. No SQLite migration. No registry change.
- **Validation**: N must be a positive integer (≥1). Match the resolver's existing guardrail at `packages/cli/src/config.ts:167` (`Number.isFinite(raw) && raw >= 1`). Reject non-numeric, zero, negative, fractional with a clear stderr message and non-zero exit.
- **Agent targeting**: accept optional `--agent <name>` flag. When supplied, resolve the agent root via `resolveAgentRoot()` at `packages/cli/src/registry.ts:122` (handles both registry name and path-like inputs). When omitted, default to CWD via `getRoot()`. Reject remote agents (registry entries with `type: 'remote'` / no `root`) with a clear error — identity.yaml mutation requires local filesystem access.
- **Persistence + propagation**: `getIdentityForRoot` caches per root (`packages/cli/src/config.ts:304`), but `worker-heartbeat.ts:211` and `ceo-heartbeat.ts:357` re-import `config.js` and call the resolver per run, and there's already an `identity-watcher` plus `clearIdentityCache(root)` helper at `config.ts:333`. The setter MUST call `clearIdentityCache(root)` after writing so the next heartbeat in the same process sees the new value (no restart required).
- **`get-turns` semantics**: print the *effective* value (resolver result from `getHeartbeatMaxInnerTurnsForRoot(root)`), with a note when the value is the default-50 fallback vs. an explicit identity.yaml setting.
- **Output**: setter prints `PRIMARY` `Set inner turn limit to N for agent <name>` plus a `DIM` `Takes effect on next heartbeat.` line — mirrors the `kyberbot bus subscribe` output convention at `commands/bus.ts:389-390`.
- **Help text disambiguation** (mandatory): `--help` for `set-turns` and `get-turns` must list (a) the exact identity.yaml field, (b) the default (50), (c) the two consumers (worker heartbeat, CEO heartbeat), and (d) what is NOT controlled (worker continuation loop `worker_max_turns`, sub-agent `maxTurns` — that one has its own setter via `kyberbot agent create --max-turns`).

## RESEARCH

**Feasibility: high.** Re-target is a write-only delta on top of an existing read path. The resolver, identity.yaml, the cache-invalidation helper, and an established read-mutate-write idiom for identity.yaml all exist. Estimated change: ~80 lines in `packages/cli/src/commands/fleet.ts` plus a unit test.

**Re-targeted read site (verified):** `getHeartbeatMaxInnerTurnsForRoot()` at `packages/cli/src/config.ts:164-170` reads `heartbeat_max_inner_turns` from identity.yaml, validates `Number.isFinite(raw) && raw >= 1`, and falls back to 50. Field is declared optional `number` at `packages/cli/src/types.ts:54`. Two consumers: `packages/cli/src/orchestration/worker-heartbeat.ts:212` (`maxInnerTurns`) and `packages/cli/src/orchestration/ceo-heartbeat.ts:357,361` (passed as `maxTurns` to `client.complete`). Both re-resolve via dynamic `import('../config.js')` per run.

**Mutation idiom (verified pattern to mirror):** `packages/cli/src/commands/bus.ts:370-388` (subscribe) and `commands/bus.ts:407-423` (unsubscribe) — `readFileSync` → `yaml.load as Record<string, unknown>` → mutate → `writeFileSync(path, yaml.dump(identity, { lineWidth: 120 }), 'utf-8')`. Same pattern in `commands/channel.ts:139,157,178`, `commands/onboard.ts:226`, `commands/update.ts:335`, and `templates/auto-migrate.ts:71`. Lock-step with this convention; no need to introduce a new helper module.

**Cache invalidation (verified):** `clearIdentityCache(root)` at `packages/cli/src/config.ts:333` exists and is exported. Must be called after the write so a same-process heartbeat picks up the new value without a restart. The `identity-watcher` (referenced by `runtime/identity-watcher.test.ts`) also fires on filesystem changes, so any out-of-process consumers (running fleet servers) will refresh on their next reload tick.

**Agent-root resolution (verified):** `resolveAgentRoot(nameOrPath)` at `packages/cli/src/registry.ts:122` already handles both name lookup (via `~/.kyberbot/registry.yaml`) and path inputs (`./`, `~/`, `/`, `.`). Throws with a clear message if the name is not registered. Remote agents have `type: 'remote'` and no `root` field (`registry.ts:212-219`); the new command must check this explicitly and bail before attempting to read identity.yaml.

**Identified risks / footguns:**
1. **Naming collision (still applies, now critical).** Three turn-counters exist — `worker_max_turns` (default 5), `heartbeat_max_inner_turns` (default 50, this command's target), sub-agent `maxTurns` (default 10). Help text must spell out the field name and explicitly disclaim the other two, otherwise operators will assume a "turn limit" CLI controls all three.
2. **"Fleet" wording is a slight mislabel.** The command writes a per-agent identity.yaml field, scoped to the agent at CWD or `--agent <name>`. There is no fleet-wide override. Help text should say "for the targeted agent."
3. **Hardcoded turn caps elsewhere.** `maxTurns: 30` in `server/channels/telegram.ts:130` and `whatsapp.ts:88`, and `maxTurns: 1` in brain extractors are unrelated and out of scope. Help text should not promise to affect them.
4. **Cache invalidation must not be skipped.** If the setter forgets `clearIdentityCache(root)`, an in-process heartbeat will continue to use the old cached identity until process restart — making the "takes effect on next heartbeat" promise false in long-running fleets.
5. **Concurrent writes.** identity.yaml is also mutated by `bus subscribe/unsubscribe`, `channel` setters, `onboard`, and `update`. No file locking exists today; concurrent CLI invocations could interleave. Out of scope for this job (matches the rest of the codebase's posture), but worth noting.

**Patterns to lean on:**
- `kyberbot bus subscribe` (`commands/bus.ts:370-394`) — read/mutate/write identity.yaml + "takes effect on next start" output line.
- `kyberbot agent create --max-turns` (`commands/agent.ts:80`) — `parseInt` argument parsing precedent.
- `kyberbot fleet defaults` (`commands/fleet.ts:517-545`) — same-subcommand "set when arg supplied, show when not" branch as a backup pattern (we are doing two separate subcommands per operator's Q3, but this is a fallback if we ever consolidate).
- `resolveAgentRoot` (`registry.ts:122`) — agent-name → root resolution for `--agent`.
- `clearIdentityCache` (`config.ts:333`) — same-process cache invalidation after the write.

**Out of scope (confirmed):** dispatcher `maxTurns` in `runtime-manifest.config.json` (ALS-side, separate subsystem); sub-agent `maxTurns` (already has setter via `agent create --max-turns`); `worker_max_turns` (operator deferred via Q1 — can be a follow-up job); hardcoded channel handler turn caps; SQLite `orchestration_settings` schema changes (operator chose Q2 → A, stay in identity.yaml); `kyberbot orch settings` mirror command (operator chose Q3 → A; can be a follow-up job).

## RESEARCH_QUESTIONS

null

## PLAN

### Summary

Add two sibling subcommands to the existing `kyberbot fleet` group: `set-turns <N>` (writer) and `get-turns` (reader). Both target a single per-agent identity.yaml field — `heartbeat_max_inner_turns` — resolving the agent root from `--agent <name>` (via `resolveAgentRoot`) or CWD (via `getRoot`). The setter validates a positive integer, performs a read-mutate-write on identity.yaml using the established `bus subscribe` idiom, then calls `clearIdentityCache(root)` so the next in-process heartbeat sees the new value. The reader prints the resolver-effective value with a marker noting whether it came from an explicit identity.yaml setting or the default-50 fallback. Help text on both subcommands explicitly disambiguates this knob from `worker_max_turns` and per-agent sub-agent `maxTurns`.

### Implementation Changes

**`packages/cli/src/commands/fleet.ts`** — single-file change. Add two subcommands between the existing `defaults` block (ends at line 545) and `return fleet;` (line 547).

1. **Imports.** No new top-level imports needed — `readFileSync`, `writeFileSync`, `existsSync`, `join`, `yaml`, `chalk` are already imported. Pull in additionally:
   - `resolveAgentRoot` from `../registry.js` (extend the existing import block at lines 21–29).
   - `loadRegistry` is already imported; reuse it for the remote-agent guard.
   - Dynamic `import('../config.js')` inside each action (matching the `bus.ts` pattern) to grab `getRoot`, `getHeartbeatMaxInnerTurnsForRoot`, `clearIdentityCache`. Dynamic import keeps the eager surface of `fleet.ts` slim and avoids circular-import risk with `config.ts`.

2. **Shared helper (file-private).** Add a small `resolveTargetRoot(agentFlag?: string): { root: string; name: string }` helper that:
   - If `agentFlag` is provided, calls `resolveAgentRoot(agentFlag)`. Then loads the registry, finds the entry whose `root === resolved` (case-insensitive name match via `getAgentNameFromRoot` for path inputs), and inspects `entry.type === 'remote'`. If the entry is registered as remote, error and exit non-zero with `Cannot mutate identity.yaml on remote agent "<name>". Set the turn limit on the agent host instead.`
   - If `agentFlag` is omitted, calls dynamic-imported `getRoot()`.
   - Confirms `existsSync(join(root, 'identity.yaml'))` and errors if missing.
   - Resolves the display `name` via `getAgentNameFromRoot(root)` (already imported) for use in output.

3. **`fleet set-turns <N>` subcommand.**
   ```
   .command('set-turns')
   .description('Set the inner Claude SDK turn cap (heartbeat_max_inner_turns) for the targeted agent. Default 50.')
   .argument('<n>', 'Maximum inner turns per heartbeat (positive integer)')
   .option('--agent <name>', 'Target agent (registry name or path). Defaults to current directory.')
   ```
   Help-text addendum (printed via `.addHelpText('after', ...)`):
   ```
   Field: heartbeat_max_inner_turns in <agent>/identity.yaml
   Default: 50
   Consumers:
     - worker heartbeat (packages/cli/src/orchestration/worker-heartbeat.ts)
     - CEO heartbeat (packages/cli/src/orchestration/ceo-heartbeat.ts)
   Does NOT affect:
     - worker_max_turns (outer worker continuation loop, default 5)
     - per-agent sub-agent maxTurns (use `kyberbot agent create --max-turns` or edit agents/<name>/agent.yaml)
   ```
   Action body:
   1. Validate `<n>`: parse with `Number(n)`. Reject if `!Number.isInteger(parsed) || parsed < 1` with stderr message `Invalid turn count "<n>". Must be a positive integer (≥ 1).` and `process.exit(1)`. Use `Number.isInteger` (not parseInt) so `"5.5"` and `"abc"` both fail.
   2. `const { root, name } = resolveTargetRoot(options.agent)`.
   3. Read identity.yaml: `const raw = readFileSync(join(root, 'identity.yaml'), 'utf-8'); const identity = yaml.load(raw) as Record<string, unknown>;`.
   4. Mutate: `identity.heartbeat_max_inner_turns = parsed;`.
   5. Write: `writeFileSync(identityPath, yaml.dump(identity, { lineWidth: 120 }), 'utf-8')`.
   6. Invalidate cache: `const { clearIdentityCache } = await import('../config.js'); clearIdentityCache(root);`.
   7. Output:
      - `console.log(PRIMARY(\`Set inner turn limit to \${parsed} for agent \${name}\`));`
      - `console.log(DIM('Takes effect on next heartbeat.'));`
   8. Wrap the entire body in `try/catch` mirroring `bus.ts:391-394`.

4. **`fleet get-turns` subcommand.**
   ```
   .command('get-turns')
   .description('Show the effective inner Claude SDK turn cap (heartbeat_max_inner_turns) for the targeted agent.')
   .option('--agent <name>', 'Target agent (registry name or path). Defaults to current directory.')
   ```
   Same help-text addendum as `set-turns`.
   Action body:
   1. `const { root, name } = resolveTargetRoot(options.agent);`
   2. Read raw identity.yaml to determine whether the field is explicitly set: load, check `typeof identity.heartbeat_max_inner_turns === 'number' && Number.isFinite(identity.heartbeat_max_inner_turns) && identity.heartbeat_max_inner_turns >= 1`. Capture as `isExplicit`.
   3. Compute effective: `const { getHeartbeatMaxInnerTurnsForRoot } = await import('../config.js'); const effective = getHeartbeatMaxInnerTurnsForRoot(root);`.
   4. Output:
      - `console.log(PRIMARY(\`\${effective}\`) + \` inner turns for agent \${name}\`);`
      - If `isExplicit`: `console.log(DIM('Source: heartbeat_max_inner_turns in identity.yaml'));`
      - Else: `console.log(DIM('Source: default fallback (heartbeat_max_inner_turns not set in identity.yaml)'));`

5. **No changes outside `fleet.ts`.** No edits to `config.ts`, `types.ts`, `registry.ts`, `worker-heartbeat.ts`, `ceo-heartbeat.ts`, `db.ts`, `settings.ts`, or any HTTP API. The resolver and field are pre-existing; this job only adds the CLI surface.

### Public Interface / Behavior Changes

- **New CLI:** `kyberbot fleet set-turns <N> [--agent <name>]` and `kyberbot fleet get-turns [--agent <name>]`.
- **identity.yaml side effect:** writing `set-turns` adds or updates a top-level `heartbeat_max_inner_turns: <N>` key. No other keys are touched, but `yaml.dump` will normalize quoting/spacing per `js-yaml` defaults — same as every other identity.yaml mutation today.
- **Runtime effect:** in-process heartbeats (worker + CEO) on the target agent will pick up the new cap on their next call, without process restart, because `clearIdentityCache(root)` is invoked. Out-of-process consumers refresh on the existing `identity-watcher` filesystem tick.
- **No removal or rename of any existing surface.** `kyberbot fleet defaults`, `kyberbot orch ...`, and `kyberbot agent create --max-turns` are untouched.

### Test Plan

A unit test is added at `packages/cli/src/commands/fleet.test.ts` (new file, mirrors the test scaffolding in `commands/search.test.ts` and `commands/token.test.ts`). Because the `fleet.ts` action bodies are not currently exported, the unit test will exercise the I/O contract directly:

1. **Validation:** factor `parsed` validation into a `parseTurnCount(input: string): number` exported helper inside `fleet.ts` (the only newly exported symbol for testability). Test cases: `"1" → 1`, `"50" → 50`, `"abc" → throws`, `"0" → throws`, `"-3" → throws`, `"5.5" → throws`, `"" → throws`, `" 5 "` (trim handling — pick a behavior and document; recommend `Number()` which accepts `" 5 "`).
2. **Round-trip:** in a `tmp` directory, write a fixture identity.yaml with `agent_name: test`, invoke a small extracted writer helper `writeHeartbeatMaxInnerTurns(root, n)`, then re-read and assert the field equals `n` and other fields are preserved.
3. **Remote-agent guard:** mock `loadRegistry` to return an entry with `type: 'remote'`; assert the resolver helper throws with the expected message.

Manual verification commands (run from a real agent root, e.g. `~/kyberbot`):

```bash
# Build the CLI
pnpm --filter @kyberbot/cli build

# Effective value before any change (expect "50" + default-fallback note)
node packages/cli/dist/index.js fleet get-turns

# Set to 25 in current dir
node packages/cli/dist/index.js fleet set-turns 25

# Verify file
grep heartbeat_max_inner_turns identity.yaml

# Effective value after (expect "25" + identity.yaml source note)
node packages/cli/dist/index.js fleet get-turns

# Cross-agent variant (assumes "atlas" is a registered local agent)
node packages/cli/dist/index.js fleet set-turns 40 --agent atlas
node packages/cli/dist/index.js fleet get-turns --agent atlas

# Validation failures (each should print red error and exit 1)
node packages/cli/dist/index.js fleet set-turns abc; echo "exit=$?"
node packages/cli/dist/index.js fleet set-turns 0;   echo "exit=$?"
node packages/cli/dist/index.js fleet set-turns -3;  echo "exit=$?"
node packages/cli/dist/index.js fleet set-turns 5.5; echo "exit=$?"

# Help-text disambiguation
node packages/cli/dist/index.js fleet set-turns --help | grep -E "heartbeat_max_inner_turns|worker_max_turns|maxTurns"
```

Run the existing test suite to confirm no regressions: `pnpm --filter @kyberbot/cli test`.

### Assumptions

- The dev agent is working on the `delamain/kyberbot-factory-jobs/KF-002/...` worktree branch and will commit changes there.
- `pnpm` is the package manager (consistent with the monorepo layout under `packages/cli`); the dev agent will confirm via `package.json` and adjust commands if `npm` is in use.
- The existing `bus.ts` write idiom (read → `yaml.load as Record<string, unknown>` → mutate → `yaml.dump({lineWidth: 120})`) is the canonical pattern; no new mutation helper module is created.
- `Number.isInteger(Number(input))` is the agreed validation — stricter than `parseInt` (rejects `"5.5"`) and aligned with the resolver's `Number.isFinite && >= 1` guardrail.
- The `--agent` flag accepts both registry names and path strings (consistent with `resolveAgentRoot`); no need for a separate `--root` flag.
- A single new exported helper (`parseTurnCount`) for unit testing is acceptable; if the dev agent prefers to keep the surface fully internal, an integration test using `execa` against `dist/index.js` is an acceptable substitute.

## PLAN_QUESTIONS

null

## ARCHITECTURE

### Why two subcommands instead of one consolidated `turns` command

The operator chose this in research Q3. Two separate subcommands (`set-turns` / `get-turns`) follow the explicit-verb convention used by the broader CLI and avoid the "argument-presence overload" idiom of `fleet defaults` (which is a fallback pattern noted in research, not the precedent we're emulating). Help, completion, and error surfaces are cleaner per-verb.

### Why `kyberbot fleet ...` and not `kyberbot orch settings ...`

Research surfaced the natural split: `orch` wraps HTTP `/fleet/orch/*` endpoints (SQLite-backed orchestration runtime knobs); `fleet` wraps registry + per-agent identity.yaml concerns. `heartbeat_max_inner_turns` lives in identity.yaml, is per-agent, and is read directly from disk by both heartbeat resolvers — it never crosses the orchestration HTTP boundary. Putting the command under `fleet` keeps the data path and the command path on the same side of that boundary. (A future `orch settings` mirror command can still land separately if/when this knob is promoted into the SQLite singleton — that's deferred per research Q2.)

### Why dynamic `import('../config.js')` inside actions

Two existing fleet-adjacent commands already use this pattern (`bus.ts:369`, `bus.ts:407`). Reasons:
1. **Avoids loading `config.ts` (and its identity.yaml read) at CLI cold-start** for unrelated commands.
2. **Sidesteps the static-import circular-risk** between commands and config — `config.ts` reads agent root and identity, and several commands call into it conditionally.
3. **Mirrors the established mutation idiom**, so reviewers don't have to reason about a new pattern.

`registry.ts` is fine to import statically (already done by `fleet.ts`), so `resolveAgentRoot` goes in the existing import block.

### Why `clearIdentityCache(root)` after the write

`getIdentityForRoot` caches by root in a module-level `Map`. In a long-running fleet process (e.g. the worker spawned by `kyberbot fleet start`), the next `getHeartbeatMaxInnerTurnsForRoot(root)` call would otherwise return the cached old value until the `identity-watcher` filesystem tick fires (or until the process restarts). The `set-turns` command may be run inside such a process (operator opens a second terminal in the same agent root), so the explicit cache invalidation makes the "Takes effect on next heartbeat." promise truthful in the dominant case. For separate-process consumers, the filesystem watcher already handles propagation.

### Why reject remote agents explicitly

`resolveAgentRoot` returns `entry.root || ''` for remote entries (`registry.ts:137`), which would pass an empty string into `readFileSync` and fail with a confusing ENOENT. Catching `type: 'remote'` upfront produces a clear error message and points the operator at the right place to make the change (the remote host).

### Why `Number.isInteger(Number(input))` validation

The resolver's runtime guardrail is `Number.isFinite(raw) && raw >= 1`, which would accept `1.5` and silently `Math.floor` it. We tighten validation at the CLI boundary to reject fractions (`"5.5"`) outright — operators expressing an integer cap should not be silently coerced. `Number()` is preferable to `parseInt` because `parseInt("5abc")` returns `5` whereas `Number("5abc")` returns `NaN`.

### Why detect "explicit vs default" in `get-turns`

Operators need to distinguish "I set this to 50" from "the system defaulted to 50." Without that distinction, `get-turns` would mask whether a previous `set-turns 50` actually persisted. The check reads raw identity.yaml directly (cheap — one file read) and applies the same `typeof === 'number' && finite && >= 1` predicate as the resolver, so the displayed source label aligns precisely with which branch the resolver will take.

### Integration with existing code

- **identity.yaml schema:** field is already declared at `packages/cli/src/types.ts:54` as optional `number`. No type change.
- **Resolvers:** unchanged. `getHeartbeatMaxInnerTurnsForRoot` already exists and is the single read site.
- **Consumers:** `worker-heartbeat.ts` and `ceo-heartbeat.ts` re-resolve per heartbeat via dynamic import — no caching at the consumer layer to invalidate.
- **HTTP/API surface:** no changes. No new endpoint. The SQLite `orchestration_settings` table is untouched.
- **Cross-agent: registry-level fleet operations** (`fleet defaults --auto-start`) are orthogonal — this command never touches `~/.kyberbot/registry.yaml`.

### Concurrency posture

identity.yaml has no file-locking today; all existing setters (`bus subscribe`, `channel`, `onboard`, `update`, `auto-migrate`) accept the small interleave risk on concurrent CLI invocations. This command takes the same posture — explicitly out of scope, noted in research footgun #5. If/when locking is introduced, all setters will adopt it together.

## REVIEW

### 2026-05-06

**Outcome:** PASS

**Verification:**
- Diff scope confirmed KF-002-only against the dev-start commit (`5b738a3`): the unrelated `agent-runtime.ts` / `fleet-manager.ts` / `services/heartbeat.ts` / `KF-003.md` changes are KF-003's `lastBeat` work that landed on the shared worktree branch in between (commits 79c2159, 94fa295) and are out of scope for this review. KF-002 surface is exactly the three files PLAN promised: `kyberbot-factory/jobs/KF-002.md`, `packages/cli/src/commands/fleet.ts`, `packages/cli/src/commands/fleet.test.ts`.
- **Build:** `pnpm --filter @kyberbot/cli build` clean.
- **Unit tests:** `pnpm test:run packages/cli/src/commands/fleet.test.ts` → 7/7 passing (parseTurnCount: positive int, whitespace-padded, non-numeric, zero, negatives, fractional, empty).
- **Manual smoke test against fixture identity.yaml in `/tmp`:**
  - `fleet get-turns` (no field set) → `50 inner turns for agent test` + default-fallback source line ✓
  - `fleet set-turns 25` → wrote `heartbeat_max_inner_turns: 25` and preserved existing `agent_name`/`identity` keys; printed PRIMARY confirmation + DIM "Takes effect on next heartbeat." ✓
  - `fleet get-turns` after set → `25 inner turns for agent test` + identity.yaml-source line ✓
  - Validation: `abc`, `0`, `5.5`, `""` all hit `parseTurnCount` and exit 1 with `Error: Invalid turn count "<n>". Must be a positive integer (≥ 1).` ✓
- **Help text:** both `set-turns --help` and `get-turns --help` print the disambiguation block listing the field name (`heartbeat_max_inner_turns`), default (50), the two consumers (worker + CEO heartbeat), and the explicit "does NOT affect" disclaimer for `worker_max_turns` and per-agent sub-agent `maxTurns`. Matches REQUIREMENTS verbatim.
- **REQUIREMENTS coverage:**
  - ✓ Two subcommands `fleet set-turns <n>` / `fleet get-turns`.
  - ✓ Mutates `heartbeat_max_inner_turns` in identity.yaml only — no schema, SQLite, or registry change.
  - ✓ Validation aligns with resolver guardrail and is tightened at the CLI boundary (rejects fractions).
  - ✓ `--agent <name>` resolves via `resolveAgentRoot`; default falls back to `getRoot()`; remote-agent guard via `loadRegistry` lookup before filesystem touch.
  - ✓ `clearIdentityCache(root)` called after the write so in-process heartbeats see the new value without restart (verified in code at `fleet.ts` action body).
  - ✓ `get-turns` reports effective value via `getHeartbeatMaxInnerTurnsForRoot(root)` and labels source as explicit vs default fallback using the same `typeof === 'number' && Number.isFinite && >= 1` predicate as the resolver.
  - ✓ Output mirrors the `kyberbot bus subscribe` convention (`PRIMARY` confirmation + `DIM` "Takes effect on next heartbeat.").
- **ARCHITECTURE adherence:** dynamic `import('../config.js')` inside actions matches the `bus.ts` idiom; static import of `resolveAgentRoot` from `registry.js` extends the existing import block; no new helper module introduced; no edits outside `fleet.ts`.

**Minor observation (not blocking):** `fleet set-turns -3` is intercepted by Commander as `unknown option '-3'` before reaching `parseTurnCount`, so the error message reads `error: unknown option '-3'` instead of `Invalid turn count "-3"`. Exit code is still 1 and stderr is still clear, satisfying the requirement ("clear stderr message and non-zero exit"). Operators who want to express a negative would more naturally type `set-turns -- -3`, which does route through `parseTurnCount` and produce the canonical error. Documenting for future-proofing only — not a defect.

**Issues:** none.

## TESTS

null

## DEPLOYMENT

null

## REFERENCES

null

## ACTIVITY_LOG

- 2026-05-06: Created.
- 2026-05-06: draft-gate: sufficiency checks passed (type, PURPOSE, REQUIREMENTS all valid). Status → research.
- 2026-05-06: Status → research-gate. Research complete — 4 questions for operator (requirements need clarification).
- 2026-05-06: research-gate: operator gate required (RESEARCH_QUESTIONS contains content). Status → research-input.
- 2026-05-06: Operator answered RESEARCH_QUESTIONS. Q1 overrides recommendation (target heartbeat_max_inner_turns, not worker_max_turns). Q4 overrides recommendation (--agent flag in v1). Status → research (rework) so RESEARCH and REQUIREMENTS get re-targeted before planning.
- 2026-05-06: Status → research-gate. Research complete — requirements captured, ready for gate routing.
- 2026-05-06: research-gate: no questions detected (RESEARCH_QUESTIONS is literal null). Status → planning.
- 2026-05-06: Status → planning-gate. Plan complete — ready for gate routing.
- 2026-05-06: planning-gate: no questions detected (PLAN_QUESTIONS is literal null). Status → dev.
- 2026-05-06: Status → in-review. Implementation complete. Added `fleet set-turns <n> [--agent <name>]` and `fleet get-turns [--agent <name>]` to packages/cli/src/commands/fleet.ts. Setter validates positive integer via exported `parseTurnCount`, performs read-mutate-write on identity.yaml using the bus.ts idiom, and calls `clearIdentityCache(root)` for in-process propagation. Getter prints the resolver-effective value with a source label distinguishing explicit identity.yaml from default fallback. Help text for both subcommands lists the field, default (50), the two consumers (worker/CEO heartbeat), and explicitly disclaims `worker_max_turns` and per-agent sub-agent `maxTurns`. Remote-agent guard added in `resolveTargetRoot`. Unit test at packages/cli/src/commands/fleet.test.ts (7 tests, all passing) covers parseTurnCount validation. Manual smoke-test verified happy path + validation errors. Build clean. Two pre-existing test failures (web-api memory block, identity-watcher YAML) confirmed present on main before my changes — unrelated.
- 2026-05-06: Status → done. Review passed. Build clean, 7/7 unit tests pass, manual smoke test verified happy path + validation errors + identity.yaml round-trip, help-text disambiguation present, all REQUIREMENTS satisfied.
