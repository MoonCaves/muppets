node_modules/
dist/
*.js.map
*.d.ts.map
.env
.env.local
data/
logs/
*.db
*.db-wal
*.db-shm
.DS_Store
*.tgz
coverage/
.nyc_output/

# Agent-instance directories — never belong in upstream working tree.
# These hold per-deployment runtime state (private notes, infra docs).
# Source incident: docs/infra/ leak that triggered the May 2026 history scrub.
# Leading slash anchors to repo root — must NOT match packages/cli/src/brain/.
/brain/
/docs/infra/
